
1. r_d_devops_dz5_dir_attr.sh  -  скрипт,  который проверяет файлы в папке /etc, которые имеют аттрибут директории и выводит их файл etc_dir.txt


2. r_d_devops_dz5_exec_attr.sh  -  скрипт,  который проверяет файлы в папке /usr/bin/, находит из них файлы с атрибутом на исполнение и выводит в файл executable.txt


3. r_d_devops_dz5_var_log.sh  -  скрипт,  который выводит список файлов в папке /var/log, сортирует их по размеру и выводит имя файла и его размер в текстовый файл var_log_size.txt
